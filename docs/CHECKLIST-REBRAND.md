# Checklist — Rebrand visual (rodada Codex + continuação)

> Criado em 2026-08-20 porque a sessão atingiu ~90% de uso. Serve pra não
> perder o fio se a sessão cortar no meio — qualquer continuação (outra
> sessão, outro agente) deve ler isto primeiro pra saber exatamente o que já
> foi feito e o que falta, sem precisar redescobrir. Cada item marcado [x]
> foi verificado rodando o app de verdade (screenshot ou teste), não só
> "deveria funcionar".

## Bugs objetivos (prioridade 1 — não é gosto, é quebrado)

- [x] Tabela cortando as laterais + scroll horizontal da PÁGINA inteira (bug do `.cg-topbar` com margem negativa vazando) — corrigido, commit `34424fb`.
- [x] Tema escuro incompleto (`--color-primary-dark`, sombras, gatilho de sistema faltando) — corrigido, commit `34424fb`.
- [x] `<input type="color">` virando blob oval — corrigido, commit `34424fb`.
- [x] Botão "Nova despesa" ilegível no escuro — corrigido, commit `34424fb`.
- [x] **Sidebar não fica fixa/sticky no desktop** — causa raiz: era regressão MINHA (o `overflow-x:hidden` que eu tinha posto em `.cg-app-shell` pra outro bug fazia o navegador computar `overflow-y:auto` nele também, virando a âncora errada do `position:sticky`). Movido pra `.cg-main` (irmã da sidebar). Verificado rolando 1500px via Playwright.
- [x] **Tabela de Transações não usa a largura disponível no desktop** — a causa real (achada só agora, com screenshot do usuário mostrando o print) não era o `input[type="date"]` (aquela investigação ficou pequena demais pra fazer diferença perceptível) — era `.cg-main` (o container de todo o conteúdo, não só a tabela) com `max-width` FIXO em pixel (1180px em app.css, com uma segunda declaração morta de 1320px em components.css brigando por baixo, nenhuma das duas relativa à tela) — sobrava bastante espaço não usado nas laterais em qualquer monitor desktop enquanto a tabela ficava com scroll horizontal cortando/escondendo coluna mesmo assim. Trocado pra `max-width: min(96vw, 1600px)` (acompanha a largura real da tela, com teto só pra não esticar demais em ultrawide) + removida a declaração morta duplicada. Verificado num viewport de 1728px: tabela deixou de precisar de scroll horizontal (`scrollWidth === clientWidth`). Commit — ver "Corrige `.cg-main`..." no log.
- [x] **Cor da barra de instalação/atualização muito clara/feia** — era efeito colateral do fix de tema escuro (`--color-primary-dark` virou claro, usado como fundo sólido aqui). Trocado pra `--color-primary`. Commit `fd74324`.
- [x] **Logo de empresa/serviço nunca aparecia em "Lançamentos recentes" (Início)** — não era "só aparece se dividida", era um widget que nunca tinha lógica de logo nenhuma, só avatar-stack de pagadores. Adicionada. Commit `fd74324`.

## Pedidos de redesign (prioridade 2 — visual agressivo, item por item)

> **2026-08-20, revisão de honestidade**: os itens abaixo usavam `[~]` (parcial) pra
> misturar "o que foi verificado e está feito" com "o que não foi feito" na mesma
> linha — reação justa do usuário: "ou fez ou não fez, 0 e 1". Reescrito: cada
> `[~]` virou um `[x]` (só a parte realmente feita e verificada) + um `[ ]` novo
> (só o que falta, uma frase, sem justificativa longa). Nenhum item usa `[~]` daqui pra frente.

- [ ] Aparência geral ainda parecida demais com a versão anterior — sem exemplo/referência concreta do que "mais distinto/moderno" significa; ver `.claude/agents/product-manager.md`, item que precisa de decisão do usuário antes de ter critério de aceite.
- [x] Modal de "Nova despesa" muito poluído — seções com rótulo (Detalhes/Quem paga/Quando/Comprovante). Commit `16eecbe`.
- [x] Filtros de Transações mal posicionados (Agrupar + dimensão) — movido pro toolbar de layout/densidade, dimensão vira fileira de pills logo abaixo do cabeçalho. Commit `614db1e`.
- [x] Modos de visualização de Transações — reestruturado em 2 eixos independentes: `layout` (lista|grade) × `densidade` (normal|compacta), livremente combináveis (lista compacta agora existe). Agrupar continua terceiro filtro independente. Commit `35e40c9`.
- [x] Lista de Compras — bug do FAB sobrepondo os botões da última linha (editar/excluir/Comprar) quando a lista era curta — o espaço reservado pro FAB só existia em mobile, mas o FAB é `position:fixed` em qualquer largura. Corrigido em `css/app.css`.
- [ ] Lista de Compras — redesenho visual agressivo (cores/espaçamento/cards) além do bug acima — pendente de exemplo concreto (mesmo motivo do item de aparência geral).
- [x] Lista de Compras — "adicionar item" deve ser um modal padrão (hoje é um formulário inline expansível) — convertido pro mesmo componente `.cg-modal-backdrop`/`.cg-modal` usado no resto do app (era um card inline com um link "mais opções" pra revelar campos). De brinde: achei e corrigi um bug real de contraste no tema escuro "segue o sistema" (sem toggle manual) — o selo "R$" de qualquer `.input-group-text` ficava com fundo claro do Bootstrap + texto claro do nosso tema (ilegível), porque só `.form-control`/`.form-select` tinham override pros nossos tokens. Corrigido em `css/components.css`, afeta todo `input-group` com prefixo/sufixo no app (não só Compras). Nova regressão: `tests/e2e/compras-modal-item.spec.js`. Commit `3749860`.
- [x] Recursos — botão de editar foto (lápis) em cada card de item invisível no tema escuro (branco fixo em cima de fundo quase branco) — trocado pra `color-mix(... var(--color-surface) ...)`, mesmo truque já usado em `.cg-room-tile__edit`.
- [ ] Recursos — redesenho visual agressivo dos 3 níveis (cômodos/subcategoria/itens) — pendente de exemplo concreto.
- [x] Caixinhas — barra de progresso (`.progress` puro do Bootstrap) com trilho cinza-claro fixo, virava retângulo branco destoando no tema escuro — corrigido de uma vez pra tudo que usa `.progress` no app (afeta também "Limite de gasto" em Compras).
- [ ] Caixinhas — redesenho visual agressivo (mais alinhado/padronizado) além do bug acima — pendente de exemplo concreto.
- [ ] Perfil/Configurações — redesenho da tela inteira ("está horrível", palavras do usuário) — pendente de exemplo concreto do que incomoda especificamente na versão atual (card "IDENTIDADE" + lista "PREFERÊNCIAS" estilo iOS/Android + seção "CONTA").
- [x] Perfil — a linha "Caixinhas" deveria gerenciar bancos/imagens ali mesmo (como Empresas/Categorias), não só ser um atalho pra tela de Caixinhas. Criado `Alpine.store('caixinhaModal')` (`js/components/caixinhaManager.js`), mesmo padrão de `categoryModal`/`companyModal`: modal com formulário (nome do banco, moeda, meta, responsável, ícone preset/upload/URL) + lista "Cadastradas" com editar/excluir inline. Diferença estrutural: `categories`/`companies` vivem cacheadas em `$store.app`, mas caixinhas são carregadas localmente dentro de `caixinhasView()` (que também busca o histórico de movimentações) — não dava pra só reaproveitar `$store.app.caixinhas` porque não existe. Resolvido com um evento (`cg:caixinhas-changed`, mesmo padrão de `cg:shopping-changed`): qualquer criação/edição/exclusão pelo modal do Perfil dispara o evento, e `caixinhasView.init()` escuta e recarrega — testado editando a meta da "Nubank" pelo modal e confirmando que o valor novo aparece na tela de Caixinhas sem reload. Nova regressão: `tests/e2e/perfil-caixinhas-manager.spec.js`. A tela de Caixinhas em si (sidebar) continua existindo, focada em ver saldo/histórico e registrar aportes/retiradas.

## Ordem de execução planejada

1. Bugs objetivos (lista acima) — feito.
2. Modos de visualização de Transações (é a base estrutural que os outros redesigns de tela vão seguir) — feito.
3. Modal de despesa (reorganizar em seções/etapas) — feito.
4. Filtros de Transações — feito.
5. Compras (visualização + modal de item) — feito (ver notas acima).
6. Recursos — feito (ver notas acima).
7. Caixinhas — feito (ver notas acima).
8. Perfil/Configurações — feito (gerenciar caixinhas inline; redesenho agressivo da tela toda não teve sinal concreto pra fazer, ver nota acima).

Todos os 8 itens da lista original foram passados. O que resta são os itens `[ ]` de redesenho visual agressivo acima — todos bloqueados pelo mesmo motivo: sem exemplo/referência concreta do usuário sobre o que especificamente incomoda em cada tela.

## Rodada 2 — pedidos novos depois do checklist original (2026-08-20, mesma sessão)

O checklist acima (8 itens) foi todo concluído. Depois disso vieram mais pedidos, um atrás do outro — esta seção é o status REAL deles, sem enrolação, pra você ver o que falta e por quê.

- [x] Modal de "adicionar item" em Compras impossível de fechar/cancelar — INVESTIGADO A FUNDO, não reproduzi: testei fechar pelo X, pelo botão "Fechar" (com e sem nome digitado no campo), e clicando fora do modal — os três fecharam certinho em todas as tentativas. Se acontecer de novo, preciso saber EXATAMENTE o que você fez antes de travar (que campo preenchido, clicou em quê) pra eu conseguir reproduzir.
- [x] Logo oficial (você mandou 4 variantes: colorida/branco/preto/verde) substituindo o ícone provisório — favicon, manifest (PWA), apple-touch-icon (PNG, splash do iOS), e nos 3 lugares que o app mostra a própria marca (tela de entrada, sidebar, menu mobile). Commit `418477b`.
- [x] Logo de empresa/serviço em Transações estava ao lado do TÍTULO, não do nome da empresa — corrigido nas duas views (tabela e cards mobile). Mesmo commit `418477b`.
- [x] Bancos duplicados nas Caixinhas ("Nubank" da Matheus e "Nubank" da Beatriz sem relação nenhuma) — banco virou entidade compartilhada (nome+logo únicos, reaproveitados por qualquer caixinha). Commit `418477b`, com um bug crítico de LOGIN quebrado corrigido no mesmo commit (conta real sem a migração nova travava inteira — resolvido).
- [x] Botão "Sair do modo demonstração" (antes ficava preso lá pra sempre) — commit `f51469b`.
- [x] Barra de progresso "toda torta, uns centralizados outros não" — ancorada embaixo do card via CSS. Commit `3d4f3af`.
- [x] Itens vencidos/em falta com destaque vermelho pulsando — commit `3d4f3af`.
- [x] Separar "gerenciar banco" (nome+logo) de "criar/editar caixinha" (meta/moeda/responsável) — antes misturado no mesmo modal, agora dois modais: "Perfil → Bancos" / "Gerenciar bancos" (dentro de Caixinhas) só lista+form de banco; "Nova/editar caixinha" só o resto, com um seletor de banco (+ "Novo banco…" abre o formulário de banco EMPILHADO por cima, modal sobre modal). Commit `3d4f3af`.
- [x] **"Isso deve ser componentizado pra funcionar com Categorias e Empresas também"** — mesmo padrão de modal empilhado (lista → formulário por cima) aplicado a Categorias e Empresas, igual Bancos. De brinde: dedup também (nome já existente reaproveita em vez de duplicar). Commits `0927bae` (stores) + `f977b2a` (correção do HTML, que tinha ficado dessincronizado do novo estado das stores no primeiro commit — pego e corrigido na mesma sessão, testado com Playwright antes de seguir).
- [x] Tabela de Transações não aproveitava a largura da tela (sobrava espaço nas laterais, tabela cortava coluna mesmo assim) — `.cg-main` tinha `max-width` fixo em pixel (1180px), trocado pra `min(96vw, 1600px)`. Commit `01c1c0a`.
- [x] Importação de CSV de Recursos duplicando item já existente — agora pula (não duplica) item com mesmo nome no mesmo cômodo/subcategoria. "Nenhum item tem foto" não é bug: o CSV de exemplo nunca teve `foto_url` preenchida de propósito (app não hotlinka imagem de fora). Commit `8f47cdc`.
- [x] **"Modo demonstração deve ter dados 100% fakes, mockados, com todos os exemplos possíveis cadastrados"** — `js/data/mockDb.js`: 18 empresas + 4 bancos com logo (SVG embutido, nunca hotlinkado — ver `logoDataUri()`), `empresa_servico` preenchido em quase toda transação, "Financiamento Casa" agora dividida 60/40 (Matheus/Beatriz — sem isso o avatar-stack "Dividido" e o saldo "Entre vocês" nunca apareciam sem o usuário criar uma despesa dividida na mão), "Geladeira nova" parcelada (3/10 — nenhum outro lançamento do seed usava parcela), caixinha "Wise" em USD (só existia BRL até então, nunca demonstrava a conversão pra BRL). Testado via Playwright: logos aparecem em Transações/Caixinhas, split funciona, parcela mostra "3/10", conversão de moeda mostra "$300,00 ≈ R$1.558,08". Commit `652f1d8`.
- [x] Modal de lançamento (despesa/receita) "muito poluído" — pedido de rodadas anteriores que ainda não tinha reduzido volume de verdade (só organizado em seções). "Entrada rápida" perde o rótulo próprio (vira placeholder), todo o bloco expansível vai de `mb-3` pra `mb-2`, e o texto de Comprovante (label redundante + parágrafo de 2 frases) vira uma legenda de uma linha com tooltip. Comprovante+Observações+Salvar agora cabem numa tela sem scroll extra. Commit `06faead`.
- [x] **"Botão 'Ver mais' nos gráficos, abrindo um dashboard completo estilo PowerBI"** — botão "Ver mais" na Home abre `.cg-modal--full` ("Dashboard completo"): reaproveita o MESMO `dashboardView()` que já alimentava os 3 gráficos escolhíveis (não é tela nova com dado próprio) — mesmos filtros dinâmicos (Eu/membro/Grupo + período com atalhos/intervalo personalizado), uma linha de KPIs, e TODOS os recortes de uma vez (categoria/empresa/membro/entrada×saída/dia/mês/ano) em vez de só 3 escolhíveis, cada um com Chart.js (já integrado, sem lib nova). Verificado em desktop e mobile via Playwright, sem erro de console. Commit `370e655`.
- [x] Widget "Lançamentos recentes" (Início) — avatar e ordem da legenda — avatar da pessoa sempre visível (não só quando dividida), logo de empresa vira selo pequeno no canto do avatar (não substitui mais a foto), legenda reordenada (empresa/serviço → nome, com badge de categoria movido pro lado do status à direita depois de um ajuste — linha tinha ficado alta demais com o badge na legenda). Commits `6e6200a` + `0927bae`.
- [x] Importação de CSV de Recursos duplicando item existente — agora pula (não duplica) mesmo nome no mesmo cômodo/subcategoria. "Nenhum item tem foto" não é bug — CSV de exemplo nunca teve `foto_url` preenchida de propósito. Commit `8f47cdc`.
- [x] Home: "Entrada"/"Entrada prevista", "Saída"/"Saída prevista" — antes computeSummary somava tudo (pago ou não) junto; agora os números principais contam só o que tem `data_pagamento` (pago de verdade), e "Previsto: RX" aparece como legenda mostrando o total incluindo pendente. Commit `81b03dd`.
- [x] Mobile: botões de toolbar gigantes (Transações e Compras) + "modo compacto não funciona no mobile" — reduzida a escala de botões secundários no mobile; causa raiz da densidade achada e corrigida (duplicidade de regra `.cg-card` no CSS anulava o padding da variante compacta havia tempo, não só agora). Commit `00871bf`.

## Rodada 3 — pedidos novos depois da Rodada 2, com print de conta real (2026-08-20, mesma sessão)

- [x] Cards de estatística das Caixinhas (Guardado/Retirado/Saldo/Meta) com altura diferente entre si — nenhum preenchia a coluna que o `.row` do Bootstrap já estica por padrão. `height:100%` resolve. Verificado medindo os 4 cards: 120px de altura idêntica.
- [x] Barra de progresso da grade de Caixinhas não ficava fixa no fundo do card (posição variava conforme quanto texto tinha acima) — a classe Bootstrap `mt-1` (`!important`) vencia o `margin-top:auto` que deveria empurrar a barra pro fundo. Removida a classe conflitante.
- [x] Botões "Pausar"/"Retomar" (Compras) numa família visual diferente de "Iniciar/Encerrar Compra" (retângulo com borda vs. pílula preenchida, lado a lado) — nova variante `.cg-shopping-toggle--outline`, mesma pílula/altura, só sem preenchimento.
- [x] Ícone do app instalado ficava desatualizado no desktop (mobile pegou o logo novo, desktop não) — cache de ícone de PWA instalada é separado do cache normal, trocar só os bytes na mesma URL não bastava. Adicionado `?v=2` nos 3 ícones (manifest + index.html + sw.js) + `CACHE_NAME` bumpado. Limite real: atalho de desktop já instalado ANTES deste fix pode continuar com o ícone antigo "assado" no SO — só resolve desinstalando/reinstalando o app uma vez; não é algo que o código consiga forçar à distância.
- [x] Transações: segmentado "Agrupar" (Período/Responsável/Movimentação/Categoria) esticando cada botão pra ~50% da largura no mobile (bug puro do Bootstrap: `.btn-group > .btn` tem `flex:1 1 auto` por padrão, `.flex-wrap` fazia cada linha de 2 esticar 50/50) — escopado numa classe nova (`.cg-agrupar-toggle`, `flex: 0 0 auto`), sem tocar nos outros `.btn-group` do app. Verificado: os 4 botões agora têm largura própria de conteúdo (64–106px), 3 cabem numa linha.
- [x] Seletor de período (Home + Dashboard completo) sem opção de Trimestre/Semestre — adicionadas nos dois selects (mantidos em sync), lógica em `periodoRange`/`periodoLabel` (`js/components/dashboard.js`) com a mesma aritmética inteira de ano/mês já usada ali. Verificado: trimestre atual e semestre atual calculam a data certa.
- [x] Transações: visão de tabela/planilha (scroll horizontal E vertical) disponível também no mobile — novo modo de layout `'tabela'` (terceiro botão no `.cg-view-toggle`), reaproveita a mesma marcação de tabela do desktop dentro de uma caixa com altura travada (`.cg-scroll-xy`, cabeçalho fixo). Verificado: `scrollWidth` 1154px vs. `clientWidth` 342px (scroll horizontal existe) e `scrollHeight` 1914px vs. `clientHeight` 549px (scroll vertical existe).
- [x] Lista de Compras no mobile: botões grandes demais, chrome de controles dominando a tela acima da lista real, faltando um botão de Cancelar claro — padding/gap do painel reduzidos só no mobile (desktop intacto), `.cg-shopping-toggle` de 48px/0.95rem pra 42px/0.82rem, e novo botão "Cancelar" (`cancelarPausada()`, `js/components/shoppingList.js`) visível só quando a lista está pausada — antes "Iniciar Compra" e "Retomar" faziam a mesma coisa (retomar), sem nenhuma saída pra abandonar de vez uma compra pausada. Verificado com screenshot: item da lista já aparece dentro do viewport de 390×844 (antes só chrome de controle aparecia).
- [x] Recursos/Home: último item da lista "Em falta"/"Sugestões" com o rodapé cortado — os 3 containers com `overflow-y:auto`+`max-height` desse padrão não tinham `padding-bottom`, então o último item ficava com a borda encostada exatamente no limite do scroll. Adicionado `padding-bottom: var(--space-2)` nos 3.
- [x] Home: botões de ação rápida (Nova despesa/Despesa fixa/Nova entrada/Lista de compras) não cabiam numa linha só no mobile — escopado só à Início (`section[x-data^="dashboardView"] .cg-quick-actions`, não a regra `.cg-quick-actions` genérica, que também é usada em Transações/Caixinhas com outra quantidade de botões) — 4 colunas numa linha, ícone empilhado sobre o rótulo. Verificado com screenshot: os 4 cabem numa linha em 390px.
- [ ] "Ver mais" (Dashboard completo) considerado abaixo do esperado ("PowerBI profissional") — precisa de direção mais concreta do usuário sobre o que falta (mais gráficos? drill-down? exportar? comparação lado a lado?) antes de eu adivinhar de novo.

## Rodada 5 — auditoria de documentação + botão voltar físico (2026-08-21, mesma sessão)

- [x] Auditei `.claude/docs/roadmap.md` (que eu mesmo escrevi) contra o código de verdade — 5 itens marcados como pendentes já estavam implementados (Money Engine em centavos, alternativa textual em gráfico, severidade de notificação, `prefers-reduced-motion`, "quanto ainda posso gastar" na Home). Corrigido antes de gastar esforço (meu ou de agente) resolvendo problema que não existe mais.
- [x] Botão físico "voltar" (Android)/gesto do navegador fechava o app direto em vez de andar pelas telas — `setView` usava `history.replaceState` (só mantinha a URL sincronizada, nunca empilhava histórico). Trocado por `pushState` + sync via `popstate`. Verificado com `goBack()`/`goForward()` reais via Playwright, não só clique de botão. Novo teste: `tests/e2e/navegacao-historico.spec.js`.
- [x] `.claude/language.md` (preferência de idioma solta, não executável por si — não é formato de agente/comando) formalizada em `CLAUDE.md`: responder sempre em PT-BR.

## Rodada 4 — notificação push, Compras/Transações, feature de Cartão de Crédito (2026-08-20, mesma sessão)

- [x] Notificação push mostrando um retângulo branco em vez do logo — o campo `badge` (selo mono cromático que o SO extrai só pelo canal alfa) usava o mesmo PNG opaco do `icon`, sem transparência real. Gerado `assets/icons/badge-mono.png` (mesma marca, sem fundo, alfa de verdade — confirmado via PIL: canto 100% transparente, ~24% dos pixels cobrindo só o desenho).
- [x] Compras "Cancelar" estava encerrando a lista pausada e criando uma nova em branco — pedido explícito: "não deve limpar a lista, só limpa se encerrar". Novo status "planejando" (sem finalizar, sem lista nova) — verificado: mesma contagem de itens antes/depois de cancelar.
- [x] Transações agrupado por Período: mês atual não ficava garantido no topo — mês FUTURO (recorrência já gerada com antecedência) ordenava antes por comparação de string ("2026-09" > "2026-08"). Mês atual agora sempre primeiro, sem exceção.
- [x] **Feature nova: despesa "no cartão de crédito"** — categoria "Cartão de crédito" vira "fatura" (pai de accordion) em Transações; qualquer despesa marcada `cartão de crédito = true` no mesmo mês vira "filha", visível dentro do accordion mas **excluída dos totais/gráficos** (evita a duplicação relatada: R$19,90 solto + R$2.000 que já contém esse 19,90 = R$2.019,90 errado). `groupCartaoCredito`/`isCartaoCreditoBill` (`js/services/transactions.js`), aplicado em `dashboard.js` (todo número da Home/gráficos/Dashboard completo) e `transactionTable.js` (accordion + totais do "Agrupar"). Recorrência carrega a marcação pro mês seguinte (assinatura no cartão continua no cartão). Deliberadamente NÃO mexe em orçamento por categoria nem em "Contas a vencer" (uma dívida com data continua sendo dívida, mesmo dentro da fatura). 8 testes unitários (inclusive o número exato do relato: R$2.000, não R$2.019,90) + 2 e2e (um prova o número via UI, outro confirma que "Contas a vencer" não esconde uma dívida real). 47 unit + 23 e2e verdes.
  - Nit cosmético conhecido, não corrigido: o título "Fatura Cartão de Crédito" quebra em 2 linhas na coluna estreita da tabela desktop (a coluna título compete espaço com o chevron do accordion) — funcional, só visualmente um pouco apertado com títulos longos.
  - Limitação de design assumida: uma fatura por mês por categoria "Cartão de crédito" — 2 faturas no mesmo mês (2 cartões diferentes) fariam as despesas marcadas se agruparem todas na PRIMEIRA fatura encontrada daquele mês, não na fatura "certa". Não gera duplicidade (mesma soma total nos dois casos), só atribuição visualmente errada nesse cenário específico — se isso for um caso real seu (mais de 1 cartão), me avisa que aí vale a pena um campo explícito ligando cada compra à fatura certa em vez de casar só por mês.

## Como continuar se a sessão cortar aqui

- Tudo commitado até aqui está em `git log` na branch `bonotto-2027-blueprint`.
- Rodar `npm run test:unit && npm test` pra confirmar que nada quebrou antes de continuar.
- Servidor local: `python -m http.server 5500`, acessar `http://localhost:5500/?demo=1`.
